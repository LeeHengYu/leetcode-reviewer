import { Input } from "@chakra-ui/react";

const SearchInput = () => {
  return (
    <Input
      borderRadius={20}
      placeholder="Search questions..."
      variant="filled"
    ></Input>
  );
};

export default SearchInput;
